/*

const playerFactory = (name, marker) => {
  const _name = name;
  const _marker = marker;

  const placeMarker = (location) => {
    // place marker on board...
  };

  return { _name, placeMarker };
}

*/